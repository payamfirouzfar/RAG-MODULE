# RAG-MODULE v0.1 Requirements Matrix

This matrix converts the frozen requirements into verifiable engineering obligations and adds the architectural requirements introduced by the v0.1 direction.

| ID | Requirement | Current evidence | Gap | Planned verification |
|---|---|---|---|---|
| R1 | Vendor independence | ADR-005; no provider runtime dependency | Provider contracts not yet implemented | import/core isolation tests |
| R2 | Model independence | Provider-independence ADR | No model contract yet | provider contract tests |
| R3 | Storage independence | Requirement frozen | No storage contract yet | adapter contract tests |
| R4 | Observability independence | local Trace/Metrics/logging + execution-scoped Events | Provider integrations not yet implemented | event/adapter isolation tests |
| R5 | Evaluation first | evaluation package + step reports | Architecture evaluation contract still evolving | per-feature scorecard |
| R6 | Replaceability | Module contract | Future Component/Port contracts needed | contract compatibility tests |
| R7 | Composability | Module registration + Sequential | No general Block/Graph model | composition contract tests |
| R8 | Inspectability | Module.inspect()/repr() | No structured architecture snapshot | snapshot tests |
| R9 | Reproducibility | ExecutionContext + evaluation metadata | Run artifact schema incomplete | deterministic metadata tests |
| R10 | Security by default | redaction + sensitive-key detection | Need architecture snapshot secret policy | secret exclusion tests |
| R11 | Optional dependencies | pyproject extras direction | Provider extras not yet implemented | dependency/import CI |
| R12 | Backward compatibility | public API contract test | Migration strategy for Component missing | API contract suite |
| R13 | Performance budgets | Step 1-4 benchmarks + Step 5 benchmark protocol | Post-fix CI baseline must be collected before a hard threshold is introduced | benchmark artifact + controlled regression comparison |
| R14 | Local-first | current core is offline | Future providers must preserve offline core | offline CI path |
| R15 | Cloud-optional | no cloud requirement | Future enterprise layer undefined | packaging/import boundary tests |
| A1 | Component is the stable conceptual unit | ADR-010; `ragtorch.core.component.Component` structural protocol | Ports/Blocks/Architecture built on top of Component not yet designed | contract tests (`tests/unit/core/test_component.py`) |
| A2 | Blocks compose Components | Sequential exists | Block abstraction not yet formalized | nested composition tests |
| A3 | Architectures are user-defined | RAGModule marker exists | RAGModule semantics too weak | architecture contract |
| A4 | Runtime is separate from business logic | ExecutionEngine exists | Runtime ownership boundaries need formalization | engine/component integration tests |
| A5 | Explicit execution identity | ExecutionContext + execution-scoped Events | EventBus must eventually become execution-scoped for isolation | event identity tests + future concurrency tests |
| A6 | Provider routing is policy-driven | only provider ADR | Router/fallback absent | future policy tests |
| A7 | Local-only fallback is enforceable | local-first requirement | no routing layer | future security/policy tests |
| A8 | Evaluation can gate runtime behavior | evaluation is post-execution today | quality-gate policy absent | future routing evaluation |
| A9 | Architecture inspection has one source of truth | string inspect exists | no structured snapshot | snapshot contract tests |
| A10 | No global mutable execution state | explicit context + no global current-context mechanism | process-wide EventBus remains as compatibility delivery mechanism | future execution-scoped delivery tests |
| A11 | Core remains dependency-light | core has no provider dependency | future plugins not yet separated | import graph/packaging tests |
| A12 | Every major feature has tests + benchmark + evaluation | established through Step 4 and Step 5 benchmark/CI | must continue | milestone checklist |
| A13 | Component contract is provider-agnostic and structural | ADR-010; `Component` is a `@runtime_checkable Protocol` with `name`/`component_type`/`__call__` only | none for the minimal contract as scoped | `test_non_module_object_can_satisfy_component_protocol`, `test_incomplete_object_does_not_satisfy_component_protocol` |
| A14 | Module compatibility without inheritance migration | `Module.name`/`Module.component_type` properties; zero call-site changes | none | `test_module_subclass_satisfies_component_protocol`; full existing 148-test suite unmodified |
| A15 | External implementations satisfy Component without a Module/ragtorch dependency | `ExternalComponent`/`IntToStringComponent` test doubles satisfy `Component` with no inheritance | none for Step 6 scope; real third-party components (Retriever, etc.) not yet built | `test_non_module_object_can_satisfy_component_protocol` |
| A16 | Component conformance checks never occur on a hot execution path | ADR-010 hard rule; no `isinstance(..., Component)` in `Module.__call__`/`ExecutionEngine.execute`/`Sequential.forward` | enforcement is currently a documented rule + code review, not an automated lint/architecture-test | `benchmarks/step6_component_contract.py` documents the real cost (~0.3us) informationally; no automated guard yet |
| A17 | Port metadata is immutable and its type must be a real Python class | ADR-011; `InputPort`/`OutputPort` are frozen dataclasses, `__post_init__` rejects non-class `type` via `ValidationError` | none for the scoped contract | `test_input_port_is_immutable`, `test_input_port_rejects_non_class_type`, `test_input_port_rejects_generic_alias` |
| A18 | Port compatibility is explicit, deterministic, and conservative (exact/subtype only) | ADR-011; `is_compatible()` = `issubclass(output.type, input.type)` | generic/`Union`/structural compatibility not supported — explicit non-goal until a real component demonstrates need | `test_exact_types_are_compatible`, `test_subtype_is_compatible_with_supertype`, `test_supertype_is_not_compatible_with_subtype`, `test_unrelated_types_are_incompatible` |
| A19 | Port metadata is never derived from `__call__` annotations via reflection | ADR-011 security-motivated design; verified `typing.get_type_hints()` evaluates string annotations before deciding | none — this is a permanent constraint, not a gap | `test_ports_module_does_not_use_annotation_evaluation` (source-inspection test) |
| A20 | Ports remain optional, external metadata — not part of the `Component` contract | ADR-011 Non-goals; `Component`'s three members unchanged since ADR-010 | `Component` gaining `input_ports`/`output_ports` is future work once a real component justifies the shape | full existing Component test suite unmodified; `ports.py` has zero import relationship to `component.py` |
| A21 | Canonical architecture snapshot — one source of truth for architecture structure | ADR-012; `ragtorch.core.inspection.ArchitectureSnapshot` built by `snapshot()`, consumed by `inspect()` rather than a second independent tree walk | no serializer/renderer beyond `inspect()`'s text form yet — explicit non-goal until a real consumer needs one | `test_snapshot_matches_actual_module_hierarchy`, `test_inspect_output_unchanged_for_realistic_pipeline` |
| A22 | Deterministic node identity | ADR-012; `ArchitectureNode.id` reuses `named_modules()`'s existing dotted-path scheme — not `id()`, not a generated UUID | none for the scoped contract | `test_snapshot_uses_named_module_paths`, `test_snapshot_is_deterministic` |
| A23 | Snapshot is immutable | ADR-012; `ArchitectureNode`/`ArchitectureChild`/`ArchitectureSnapshot` are frozen dataclasses with tuple fields | none | `test_architecture_node_is_immutable`, `test_architecture_child_is_immutable`, `test_architecture_snapshot_is_immutable` |
| A24 | Structural child relationships only — no inferred data-flow edges | ADR-012 Non-goals; `ArchitectureChild` (deliberately not "Edge") represents tree containment only | data-flow/graph edges deferred until a real composition/graph contract exists (future step) | `test_snapshot_does_not_infer_data_flow_edges` |
| A25 | Ports on architecture nodes remain optional and are never auto-discovered | ADR-012; `ArchitectureNode.input_ports`/`output_ports` default to `()`; no `vars()`/`__dict__`/attribute-scanning discovery mechanism exists or was added | a future explicit port-supplying API is deferred, not designed yet | `test_architecture_node_defaults_to_empty_ports` |
| A26 | `inspect()` remains backward compatible after becoming snapshot-backed | ADR-012; `Module.inspect()`'s pre-existing string output (`"Modules: N"`, `"Depth: N"`, `"name (Type)"`) is unchanged | none — existing test passes unmodified | `test_inspect_contains_counts_and_tree` (Step 1, unmodified), `test_inspect_output_unchanged_for_realistic_pipeline` (Step 8) |
| A27 | Snapshot construction never executes a component | ADR-012; `snapshot()` reads only `name`/`component_type`/`named_children()`, never calls `module(...)` | none — this is a permanent constraint, not a gap | `test_snapshot_does_not_execute_components` (a component whose `__call__`/`forward` raises if invoked still snapshots successfully) |
| A28 | Snapshot construction never exposes arbitrary instance state | ADR-012; only known, intentional properties are read — never `vars()`/`__dict__` | none — stronger than a redaction filter: the state is structurally unreachable, not merely hidden after being read | `test_snapshot_does_not_expose_arbitrary_instance_state` (a component with `self.api_key`/`self.password` never leaks them) |
| A29 | Architecture snapshots are structurally validatable | ADR-013; `ragtorch.core.architecture.validate_snapshot()` — a non-empty-rooted-tree contract, `ValidationError` on the first violation, `None` on success | validated at the snapshot boundary only; no `Module.validate()` convenience method yet (deferred until repeatedly needed, mirroring `Module.snapshot()`'s own history) | `tests/unit/core/test_architecture.py` (19 tests), `tests/integration/test_architecture_validation.py` (3 tests) |
| A30 | Node identifiers are unique within a valid snapshot | ADR-013 Rule 2 | none | `test_duplicate_node_id_is_invalid` |
| A31 | A valid architecture contains exactly one root | ADR-013 Rule 6 | none | `test_multiple_roots_is_invalid`, `test_cycle_produces_zero_roots_is_invalid` |
| A32 | Each non-root node has exactly one parent | ADR-013 Rule 5 — proven insufficient to infer from root-count alone via a concrete diamond counter-example (`A→B`, `A→C`, `B→D`, `C→D`: one root, zero duplicate IDs, but `D` has two parents), verified by direct construction before being frozen into the ADR | none | `test_diamond_multiple_parents_is_invalid` |
| A33 | All nodes are reachable from the root | ADR-013 Rule 7 — proven to catch a disconnected cycle (`A→B` valid + `C→D→C`) that root-counting alone misses, verified by direct construction | none | `test_disconnected_node_is_invalid`, `test_disconnected_cycle_is_invalid` |
| A34 | A valid architecture is acyclic | ADR-013 — no separate cycle-detection algorithm; proven (not merely asserted) that Rules 2/5/6 catch a connected cycle via zero-roots, and Rule 7 catches a disconnected cycle | none — deliberately not implemented as a redundant separate DFS pass | `test_cycle_produces_zero_roots_is_invalid`, `test_disconnected_cycle_is_invalid` |
| A35 | Architecture validation is provider-independent | ADR-013; `architecture.py` imports only `ragtorch.core.errors`/`ragtorch.core.inspection` | none | `test_architecture_module_has_no_provider_dependencies` (AST-based, reusing the Step 7/8 pattern) |
| A36 | Architecture validation does not mutate the snapshot | ADR-013; read-only, no automatic repair/deduplication/reordering | none | `test_validation_does_not_mutate_snapshot` |
| A37 | A compatible port pair passes composition precondition checking | ADR-014; `ragtorch.core.ports.check_connection()`, `None` on success | none | `test_check_connection_returns_none_for_exact_types`, `test_check_connection_returns_none_for_compatible_subtype` |
| A38 | An incompatible port pair raises on composition precondition checking | ADR-014; `ValidationError` (reused, no new exception type) | none | `test_check_connection_raises_for_incompatible_types` |
| A39 | Composition precondition checking follows `is_compatible()`'s subtype rule exactly, with no separate compatibility logic | ADR-014; `check_connection()`'s body is exactly `if not is_compatible(...): raise ...` — verified by reading the implementation, not merely asserted | none — this is a permanent constraint, not a gap | `test_check_connection_returns_none_for_compatible_subtype`, plus the ADR-014 design rationale itself |
| A40 | `is_compatible()`'s existing `bool` contract is unchanged by the addition of `check_connection()` | ADR-014; explicit regression pair | none | `test_is_compatible_remains_true_for_valid_pair_after_check_connection_added`, `test_is_compatible_remains_false_for_invalid_pair_after_check_connection_added` |
| A41 | Composition precondition checking does not mutate either `Port` | ADR-014; both `Port` types are frozen dataclasses, re-asserted post-call | none | `test_check_connection_does_not_mutate_ports` |
| A42 | Composition precondition checking is deterministic across repeated calls, on both success and failure paths | ADR-014 | none | `test_check_connection_is_deterministic_across_repeated_calls` |
| A43 | The precondition-check error message identifies both port names and both type names | ADR-014 | none | `test_check_connection_error_contains_output_port_name`, `test_check_connection_error_contains_input_port_name`, `test_check_connection_error_contains_both_type_names` |
| A44 | Composition precondition checking is provider-independent | ADR-014; `ports.py` imports only `ragtorch.core.errors` (unchanged from Step 7) | none | `test_ports_module_has_no_provider_dependencies` (AST-based, now covering `check_connection()` too) |
| A45 | A single data-flow connection can be persisted as data, not merely checked in the moment | ADR-015; `ragtorch.core.connection.Connection`, a frozen value type | no collection/graph type holds multiple connections yet | `test_connection_constructs_for_compatible_ports` |
| A46 | Connection directionality is enforced at runtime, not merely by type annotation | ADR-015; explicit `isinstance` checks in `__post_init__` — a correction over the ADR's first draft, which assumed annotations alone were sufficient | none — this is a permanent constraint, not a gap | `test_connection_rejects_input_port_as_source`, `test_connection_rejects_output_port_as_target` |
| A47 | Connection node identifiers must be non-empty strings | ADR-015 | none | `test_connection_rejects_empty_source_node_id`, `test_connection_rejects_empty_target_node_id` |
| A48 | Connection compatibility checking delegates to `check_connection()`, never duplicates it | ADR-015; `Connection.__post_init__`'s final step is exactly `check_connection(self.source_port, self.target_port)` | none — this is a permanent constraint, not a gap | `test_connection_delegates_to_check_connection_not_duplicate_logic` (compares error messages, not just "both raise") |
| A49 | Connection has value equality and is hashable, with no synthetic identity | ADR-015; plain frozen `@dataclass`, consistent with `ArchitectureChild`'s existing precedent | a future mutable connection registry, if ever needed, would require reopening this decision | `test_equal_connections_have_value_equality`, `test_connection_is_hashable`, `test_equal_connections_hash_equally` |
| A50 | Connection places no fan-out/fan-in cardinality constraint | ADR-015; deliberately undecided, deferred to a future collection/`Block` type | cardinality rules not yet designed | `test_multiple_connections_may_share_source`, `test_multiple_connections_may_share_target` |
| A51 | Connection construction is provider-independent | ADR-015; `connection.py` imports only `ragtorch.core.errors`/`ragtorch.core.ports` | none | `test_connection_module_has_no_provider_dependencies` (AST-based, reusing the Step 7/10 pattern) |
| A52 | A collection of nodes and connections can be validated as a whole, not just pairwise | ADR-016; `ragtorch.core.composition.CompositionGraph` | none | `test_construction_succeeds_with_unique_node_ids`, `test_empty_graph_is_valid` |
| A53 | Composition node identifiers are unique within a graph | ADR-016 invariant 1 | none | `test_construction_rejects_duplicate_node_ids` |
| A54 | Every connection in a composition references nodes that actually exist in that graph | ADR-016 invariant 2 | none | `test_construction_rejects_connection_with_unknown_source`, `test_construction_rejects_connection_with_unknown_target` |
| A55 | Composition graph node identity is independent of architecture-tree path identity | ADR-016; `GraphNode.id` is graph-local, not derived from or coupled to `ArchitectureNode.id` (ADR-012) — a Staff-review correction applied before implementation | none — this is a permanent constraint, not a gap | `test_graph_node_id_is_independent_of_architecture_path` |
| A56 | Duplicate connections within a composition are rejected | ADR-016 invariant 5 | none | `test_construction_rejects_duplicate_connections` |
| A57 | A composition input port accepts at most one incoming connection (fan-in ≤ 1) | ADR-016 invariant 6; deliberately conservative — see ADR-016 for why aggregation semantics belong to a future explicit component, not the graph | a future explicit aggregation mechanism, if ever justified | `test_construction_rejects_fan_in_to_same_target_port`, `test_construction_allows_different_target_ports_on_same_node` (proves the rule is port-level, not node-level) |
| A58 | A composition output port may feed unlimited connections (fan-out unrestricted) | ADR-016 invariant 7 | none | `test_construction_allows_fan_out_from_same_source` |
| A59 | A composition graph must be acyclic | ADR-016 invariant 8; detected via an iterative Kahn's-algorithm topological sort — a recursive DFS first draft was caught failing at 1,000 nodes by this step's own benchmark and rewritten before commit | none — deliberately not the invariant for every future RAG-MODULE graph concept (e.g. a future `Loop` primitive is explicit higher-level future work, not an exception to this rule) | `test_construction_rejects_two_node_cycle`, `test_construction_rejects_three_node_cycle`, `test_construction_rejects_disconnected_cycle`, `test_construction_accepts_valid_linear_chain` |
| A60 | Removing a composition node cannot silently leave a dangling connection | ADR-016 invariant 9 | none | `test_remove_node_rejects_if_referenced_by_connection`, `test_remove_node_succeeds_when_unreferenced` |
| A61 | Composition graph operations are immutable — functional updates return a new graph, never mutate the original | ADR-016; consistent with every prior structural type's immutability guarantee in this codebase | functional updates re-validate the full graph rather than incrementally verifying only the delta — see `evaluation/step12-evaluation.md` Limitations | `test_add_node_returns_new_graph_without_mutating_original`, `test_chained_updates_preserve_each_intermediate_graph` |
| A62 | Composition graph iteration order is deterministic | ADR-016; `nodes`/`connections` are ordered tuples, not sets | none | `test_iteration_order_is_stable_across_repeated_access`, `test_equal_graphs_iterate_in_same_order`, `test_add_node_appends_new_node_last` |
| A63 | Composition graph construction and every operation never execute a component | ADR-016; no `component.__call__` is ever invoked | none — this is a permanent constraint, not a gap | `test_construction_never_executes_component`, `test_graph_operations_never_execute_component` (both use an `ExplodingComponent` that raises if invoked) |
| A64 | Composition graph construction is provider-independent | ADR-016; `composition.py` imports only `ragtorch.core.component`/`ragtorch.core.connection`/`ragtorch.core.errors` | none | `test_composition_module_has_no_provider_dependencies` (AST-based, reusing the established pattern) |
| A65 | A valid `CompositionGraph` can be transformed into a deterministic, immutable, dependency-aware `ExecutionPlan` without executing components, depending on `ExecutionEngine`, retaining runtime objects, or mutating/retaining the source graph | ADR-017; `src/ragtorch/core/execution_plan.py`; `plan(graph: CompositionGraph) -> ExecutionPlan`, FIFO Kahn's algorithm (`collections.deque`) — corrected during a two-round 13B.3/13B.5 adversarial review before implementation (ADV-01 BLOCKER: LIFO vs. FIFO queue discipline; ADV-04 MINOR: two distinct tie-break rules, not one) | none for the scoped contract | `tests/unit/core/test_execution_plan.py` (28 tests), `tests/integration/test_execution_plan_composition.py` (5 tests), `benchmarks/step13_execution_plan.py` (10/100/1,000/10,000-node scaling, three shapes); post-merge `main` CI run `31959433145`, commit `0c3756a`, 329/329 on 3.10/3.11/3.12 |
| A66 | An `ExecutionPlan` can be run to completion via a caller-supplied `StepHandler` (given the full `ExecutionStep`, not only `node_id`), producing a `StepExecutionResult` keyed by `node_id`, with each step's handler able to see prior steps' results through an immutable `StepExecutionContext` snapshot, stopping and propagating the exact original exception immediately on handler failure with no partial result ever produced, without the executor resolving `ExecutionStep -> Component` itself, retrying, timing out, running concurrently, or depending on `ExecutionEngine`, `Component`, `CompositionGraph`, or any provider | ADR-018 (Accepted); `src/ragtorch/core/execution.py` — `Executor`/`StepHandler` (`Protocol`s, synchronous-only), `SequentialExecutor`, `StepExecutionContext`, `StepExecutionResult` — failures propagate unwrapped and un-copied (`exc_info.value is error`), a considered departure from `Module.__call__`'s convention because the executor invokes an opaque caller-supplied handler, not a fixed `Component` contract (ADR-018 Q11); context-construction is an accepted, benchmark-measured O(n²) cost in v0.1, confirmed across two independent local runs (time/step grew ~780x in one run and ~560x in another from 10 to 100,000 steps — absolute wall-clock varies with machine load, but the growing-time/step shape is consistent both times — see `evaluation/step14-evaluation.md` "Performance"); post-merge `main` CI run [31971031719](https://github.com/payamfirouzfar/RAG-MODULE/actions/runs/31971031719), commit `79b1d84`, 358/358 on 3.10/3.11/3.12 | none for the scoped contract | `tests/unit/core/test_execution.py` (27 tests: CTX/EXEC/FAIL/STATE/ADV/CONTRACT), `tests/integration/test_execution_boundary.py` (2 tests: dependency-aware handler chain end-to-end via the public API, mid-plan failure propagation), `benchmarks/step14_execution_boundary.py` (10/100/1,000/10,000/100,000 steps), `evaluation/step14-evaluation.md` |
| A67 | An `ExecutionPlan` can be run via `ExecutionEngine.execute_plan(plan, handler, *, context=None, executor=None)` under the same `Run`/`Trace`/`MetricsCollector` lifecycle guarantee `execute(module, ...)` already provides for a single `Module` call — exactly one `Run`, exactly one span at `DEBUG` named `"execute_plan"`, one `"execution_plan.duration_s"` metric — with plan iteration fully delegated to a substitutable *synchronous* `Executor` (defaulting to `SequentialExecutor()`), without the engine deriving plan order, resolving `ExecutionStep -> Component` itself, making its root `ExecutionContext` reachable by `StepHandler` in any way (whose ADR-018 signature has no slot for one), or changing `execute(module, ...)` in any way | ADR-019 (Accepted); `src/ragtorch/core/engine.py` — `execute_plan` added to the existing `ExecutionEngine` class, no new module/type; reuses the engine's existing `ExecutionResult` and `_maybe_span` helper exactly as `execute(module, ...)` does; span/metric names implemented as frozen literals (`"execute_plan"`, `"execution_plan.duration_s"`) per the 15D corrections; handler is called as `handler(step, step_context)` with no additional argument, verified by a direct regression test (CTX-03) guarding against ever silently injecting `ExecutionContext`; post-merge `main` CI run [31975659964](https://github.com/payamfirouzfar/RAG-MODULE/actions/runs/31975659964), commit `f9e57dc`, 382/382 on 3.10/3.11/3.12 | none for the scoped contract | `tests/unit/core/test_engine_plan_execution.py` (21 tests: PLAN/CTX/TRACE/METRIC/FAIL/OBS/STATE), `tests/integration/test_engine_plan_boundary.py` (3 tests: dependency-aware RAG-shaped plan end-to-end via the public API, mid-plan failure, root-context-without-handler-reachability), `benchmarks/step15_plan_execution_runtime.py` (10/100/1,000 steps, isolating engine overhead from ADR-018's already-benchmarked snapshot-copying cost), `evaluation/step15-evaluation.md` |
| A68 | A `CompositionGraph` can be wrapped as a reusable `Block` — a `Module` subclass structurally satisfying `Component` with zero special case — with an explicit, required `input_node`/`output_node` pair (no first/last-node inference, no multi-root/multi-sink support in this version), delegating all execution to the existing, unmodified `plan()`/`Executor`/`ExecutionEngine.execute_plan` chain (no second execution system, no plan caching, no new exception type), such that a `Block` can itself be nested as a `GraphNode.component` inside an outer `CompositionGraph`, or as a child of a `Sequential`, with zero runtime-layer changes required for either — fulfilling A2 ("Blocks compose Components... Block abstraction not yet formalized," open since Step 1) now that every layer ADR-016 deferred it pending (`CompositionGraph`/`ExecutionPlan`/`Executor`/`ExecutionEngine.execute_plan`) is `Accepted` | ADR-020 (Accepted); `src/ragtorch/core/block.py` — `Block(Module)`, `Block.forward` builds a `StepHandler` closure resolving each node's `GraphNode.component` and calling `ExecutionEngine().execute_plan(plan(self._graph), handler, context=context)`, returning `result.output.values[self._output_node]`; nesting verified end-to-end (a `Block` as an outer `GraphNode.component`, as a `Sequential` child preserving `ExecutionContext` ancestry, and containing another `Block`) with zero changes to any protected runtime file, confirmed via explicit `git diff` on the actual CI-verified branch; benchmark shows per-call overhead growing with graph size (~14µs at 3 nodes to ~53µs at 30 nodes), consistent with `plan()`'s own O(V+E) re-derivation cost — direct evidence for ADR-020 Q11's deferred plan-caching decision, not yet acted on — see `evaluation/step16-evaluation.md` "Performance"; post-merge `main` CI run [31981239275](https://github.com/payamfirouzfar/RAG-MODULE/actions/runs/31981239275), commit `6ca35ca`, 406/406 on 3.10/3.11/3.12 | none for the scoped contract | `tests/unit/core/test_block.py` (21 tests: CONSTRUCT/CONTRACT/EXEC/NEST/FAIL), `tests/integration/test_block_composition.py` (3 tests: RAG-shaped graph end-to-end via the public API, `Block` nested inside `Sequential`, failure propagation through that nesting), `benchmarks/step16_block_composition.py` (3/10/30-node chains), `evaluation/step16-evaluation.md` |
| A69 | A `CompositionGraph` can be turned into a complete, graph-backed architecture via `RAGModule.from_graph(graph, *, input_node, output_node)`, returning an actual `RAGModule` instance (`isinstance(x, RAGModule)` and `isinstance(x, Module)` both `True`) that delegates all execution to the existing, unmodified `Block`/`plan()`/`Executor`/`ExecutionEngine.execute_plan` chain, without changing `RAGModule`'s existing class hierarchy, constructor, or subclass-and-override-`forward()` pattern, without introducing a second execution system, plan caching, serialization, async execution, or a new exception type, and without supporting `from_graph` on arbitrary `RAGModule` subclasses in this version | ADR-021 (Accepted); `src/ragtorch/core/module.py` — `RAGModule.from_graph`, private `_GraphBackedRAGModule`; four issues found and corrected before/during implementation, none silently absorbed: (1) double-registration hazard, fixed to a single `self._block = block` assignment; (2) false single-hop failure-identity assumption, corrected to the accurate walked-`__cause__`-chain contract, matching `Module.__call__`'s existing universal wrapping convention (also true of `Block` nested in `Sequential` on already-merged `main`); (3) a circular-import hazard found during implementation itself (`block.py` imports `Module` at module scope) resolved via a deferred import inside `from_graph`, mirroring `Module.snapshot()`'s existing lazy-import pattern in the same file; nesting verified end-to-end (a `Block` inside the architecture's graph, the architecture itself as a `GraphNode.component`, and as a `Sequential` child) with zero changes to any protected runtime file, confirmed via explicit `git diff` on the actual CI-verified branch; post-merge `main` CI run [31992874525](https://github.com/payamfirouzfar/RAG-MODULE/actions/runs/31992874525), commit `419f643`, 431/431 on 3.10/3.11/3.12 | none for the scoped contract | `tests/unit/core/test_ragmodule_architecture.py` (21 tests: RM/COMPAT), `tests/integration/test_ragmodule_from_graph.py` (4 tests: RAG-shaped graph end-to-end via the public API, `Sequential` nesting, `Block` node inside the architecture's own graph, failure propagation through `Sequential` nesting), `benchmarks/step17_ragmodule_architecture.py` (3/10/30-node chains), `evaluation/step17-evaluation.md` |
| A70 | A caller can observe only their own execution's (and its nested children's) lifecycle events via an explicit, opt-in `EventScope` threaded through `ExecutionContext.event_scope`, additive to the existing global `EventBus`/`event_bus()` (both destinations receive every event; no existing caller's behavior changes since `event_scope` defaults to `None`), with `child()` propagating the same `EventScope` object identity (not a fresh one) so a listener on a parent execution's scope continues to see its whole subtree — fulfilling A5 and A10's shared gap ("EventBus must eventually become execution-scoped for isolation" / "process-wide EventBus remains as compatibility delivery mechanism"), open since Step 4, now that architecture doc §5's identity invariants (already satisfied) and its delivery-isolation invariant (the actual gap) are both addressed | ADR-022 (Accepted); `src/ragtorch/core/events.py` — `EventScope` (structurally identical to `EventBus`, sharing the same `EventListener` type alias, a distinct type not a subclass); `src/ragtorch/core/context.py` — `ExecutionContext.event_scope: EventScope \| None = None`; `src/ragtorch/core/module.py` — `Module.__call__` dual-publishes to `_bus` (unconditional, unchanged) and `context.event_scope` (only when both `context` and `event_scope` are not `None`); explicit Concurrency section distinguishing `EventScope` cross-execution isolation (structurally guaranteed — proven both by independent-listener-ownership inspection and by deterministic `threading.Barrier`-synchronized concurrent tests, `EVT-ISOLATION-001`) from `EventBus` thread safety (explicitly **not** guaranteed — two formal deferred risks recorded, `EVT-RACE-001` and `EVT-FAIL-001`, neither silently absorbed nor opportunistically fixed); no production architecture change required in `Sequential`/`Block`/`RAGModule`/`ExecutionEngine` — all four already propagate `ExecutionContext` correctly, verified via 9 integration tests, not assumed; post-merge `main` CI run [32038947496](https://github.com/payamfirouzfar/RAG-MODULE/actions/runs/32038947496), commit `e83d23a`, 461/461 on 3.10/3.11/3.12 | none for the scoped contract; `EVT-RACE-001`/`EVT-FAIL-001` remain formally deferred, not resolved by this requirement | `tests/unit/core/test_events.py` (22 EVT-* contract tests: primitive/no-context/dual-delivery/child-identity/nested/sibling-isolation/ordering-non-contract/failure-short-circuit/API-parity), `tests/integration/test_execution_scoped_events.py` (13 tests: `Sequential`/nested `Sequential`/`Block`/`RAGModule`/`ExecutionEngine` propagation, failure isolation across siblings, deterministic concurrent isolation across 20 repeated runs, global-bus-current-behavior characterization), `benchmarks/step18_execution_scoped_events.py` (three-tier: no scope / empty scope / active scope, CI-executed on all 3 Python versions as a non-blocking artifact step), `evaluation/step18-evaluation.md` |
| A71 | `Module.register_module()` (and attribute-based registration via `__setattr__`, which routes through it) rejects any registration that would make a module reachable from itself — directly (self-registration) or indirectly (a cycle of any depth) — while leaving legitimate diamond-shaped sharing (the same instance under two different parents) valid, and preserving all pre-existing registration behavior (same-instance re-registration, different-instance duplicate-name rejection, non-`Module` rejection) unchanged — fulfilling A29's named cycle-registration gap, open since Step 8, and the requirements matrix's own "Next priority" item #4 | No ADR required (reasoned explicitly, not assumed — see `evaluation/step19-evaluation.md` 19D: no new type, public API, exception, or lifecycle/concurrency change); `src/ragtorch/core/module.py` — private `_would_create_cycle(parent, child)`, iterative (not recursive) identity-based (`id()`, not `==`) subtree walk, called from `register_module` only when `existing is None` so same-instance re-registration is unaffected; reuses the existing `RegistryError`, no new exception type; rejected registration verified atomic (`_modules` unmutated) by direct test, not assumed; benchmark shows registration cost growing roughly linearly with the prospective child's subtree size (3.9µs at 1 node to 959µs at 1,000 nodes), no threshold asserted; post-merge `main` CI run [32042082685](https://github.com/payamfirouzfar/RAG-MODULE/actions/runs/32042082685), commit `9c085be`, 474/474 on 3.10/3.11/3.12 | none for the scoped contract | `tests/unit/core/test_module.py` (11 new tests: direct/indirect/multi-level cycle rejection, diamond sharing remains valid, same-instance/different-instance/non-Module registration behavior all regression-tested, rejected-cycle-does-not-mutate-graph, attribute-assignment cycle rejection), `tests/integration/test_module_cycle_detection.py` (3 tests: cycle rejection through `Sequential`'s real registration path, rejected cycle leaves the pipeline traversable and executable), `benchmarks/step19_module_cycle_detection.py` (1/10/100/1,000-node subtree scaling), `evaluation/step19-evaluation.md` |
| A72 | `EventBus.publish()`/`EventScope.publish()` isolate listener failures — every listener subscribed at the start of a `publish()` call is invoked even if an earlier listener raised an `Exception`, and if any raised, `publish()` raises a single `ListenerDeliveryError(event, failures)` after every listener has run, rather than the previous accidental "first exception wins, everything after is silently skipped" behavior — fulfilling ADR-022's formally-recorded deferred risk `EVT-FAIL-001`, open since Step 18 | ADR-023 (Accepted); `src/ragtorch/core/errors.py` — new `ListenerDeliveryError`; `src/ragtorch/core/events.py` — `EventBus.publish`/`EventScope.publish` both snapshot their listener list before iterating and isolate each listener's `Exception` individually; a custom exception type was used instead of native `ExceptionGroup` because this project's CI/`pyproject.toml` both require Python 3.10 as a floor and `ExceptionGroup` requires 3.11+ (caught during 20C adversarial review, before implementation); `RecursionError` is explicitly re-raised before the generic isolation clause (applied to both `EventBus` and `EventScope` — an initial fix only covered `EventBus`, caught by a parametrized test before merge) so reentrant `publish()` still produces one clean `RecursionError` rather than a nested chain of wrapped errors (`EVT-REENTRANT-001`, newly named, deferred); non-`Exception` `BaseException` (e.g. `KeyboardInterrupt`) deliberately not caught; `EventBus`/`EventScope` thread safety (`EVT-RACE-001`) remains fully open, explicitly excluded — the ADR's own 20A audit found no new evidence beyond that risk's pre-existing record to justify acting on it now; `Module.__call__` is unmodified — empirically verified (corrected twice during drafting before checking directly) that `ListenerDeliveryError` propagates raw from all three event-type publish sites, never wrapped as `ExecutionError`; intentional, named breaking change for the narrow case of a raising listener (3 pre-existing tests rewritten to match); post-merge `main` CI run [32053185219](https://github.com/payamfirouzfar/RAG-MODULE/actions/runs/32053185219), commit `4c545a0`, 496/496 on 3.10/3.11/3.12 | `EVT-RACE-001`/`EVT-REENTRANT-001` remain formally deferred, not resolved by this requirement; `Module.__call__`'s dual-publish sequencing (ADR-022) still means a raising global-bus listener prevents the scope's `publish()` call from running at all — a named scope boundary, not fixed here | `tests/unit/core/test_events.py` (12 new parametrized `FAIL-ISO-*` tests across both `EventBus`/`EventScope`, 3 pre-existing tests intentionally rewritten, 1 new API-parity test), `tests/integration/test_execution_scoped_events.py` (4 new tests through real `Module.__call__`/`Sequential` composition paths), `benchmarks/step20_listener_failure_isolation.py` (no-failure baseline + failure-path cost, 1/10/100 listeners), `evaluation/step20-evaluation.md` |
| A73 | `EventBus`/`EventScope` concurrent subscribe/unsubscribe/publish was deterministically audited (`threading.Barrier`, never `time.sleep`, up to 20,000-iteration churn and 4-way concurrent publish) and found to have **zero memory-corruption or crash hazards** under CPython — but this is explicitly **not** a thread-safety guarantee: the library makes none, matching `SequentialExecutor`'s own existing precedent (ADR-018, "thread-safety... not claimed here"). The one real, deterministically-reproducible finding — concurrent double-`unsubscribe()` of the *same* listener races to `ValueError` in the losing thread — was traced to **pre-existing, single-threaded behavior** (`unsubscribe()` was never idempotent, with or without threads) rather than a new concurrency defect. Decision: no `ADR-024`, no synchronization primitive added — the evidence did not support it | Step 21 audit (no new ADR; `docs/architecture/decisions/ADR-022-execution-scoped-event-delivery.md`'s `EVT-RACE-001` entry updated in place with these findings, remaining explicitly **Deferred**, not resolved); zero changes to `src/ragtorch/core/events.py`/`errors.py`/`module.py` — confirmed via `git diff --stat` showing zero lines in any production file; post-merge `main` CI run [32060057851](https://github.com/payamfirouzfar/RAG-MODULE/actions/runs/32060057851), commit `ab628db`, 506/506 on 3.10/3.11/3.12. **Precise framing, not to be paraphrased as "EventBus is thread-safe":** the library does not promise thread safety; deterministic adversarial testing found no new memory-corruption/concurrency defect requiring synchronization, and the one concurrent failure observed (`ValueError` from double-unsubscribe) is pre-existing API behavior, not a new one | `EVT-RACE-001` remains fully open and Deferred (unchanged, re-confirmed rather than resolved); `EVT-REENTRANT-001` (Step 20) remains separately tracked and unaffected — Step 20's deliberate `RecursionError` propagation is not itself a designed reentrancy model, only a preserved non-goal; `unsubscribe()`'s non-idempotency remains undocumented as a public contract point (tested, not fixed) | `tests/unit/core/test_events.py` (10 new parametrized tests across both `EventBus`/`EventScope`: concurrent subscribe, concurrent publish with no-loss/no-duplication, concurrent subscribe+publish churn, concurrent double-unsubscribe race, single-threaded double-unsubscribe root cause), `benchmarks/step21_event_concurrency_audit.py` (single-threaded baseline + concurrent worker scaling, 1/2/4/8 workers, no threshold), `evaluation/step21-evaluation.md` |
| A74 | `EventBus`/`EventScope` reentrancy (a listener calling `publish()` again, directly or by invoking a real `Module`, before the outer `publish()` call returns) was audited across 15 concrete cases (R1-R15) and found to already compose correctly wherever the nested publication is of a *different*, terminating event — no special-casing needed, confirmed empirically across nested-delivery ordering, nested-failure isolation (both caught and uncaught by the listener), cross-object nesting, self-unsubscribe-then-nest, and recursion reproduced through real `Module.__call__` execution, not only synthetic direct `publish()` calls. Only unconditional recursive publication of the *same* event triggers `RecursionError`, confirmed **self-bounded and cheap** (~499 levels of legitimate nesting available before Python's default 1000-frame limit; an accidental infinite-recursion bug terminates in under 1ms, 200-sample benchmark) — not a meaningful denial-of-service vector. Five candidate policies (preserve-current / detect-and-reject / suppress / queue / bounded-depth-limit) were adversarially compared against 15 criteria; preserve-current won on every one. Decision: no `ADR-024`, no production code change — the evidence did not support any alternative to today's behavior | Step 22 audit (no new ADR; `docs/architecture/decisions/ADR-023-event-listener-failure-isolation.md`'s `EVT-REENTRANT-001` entry updated in place with these findings, remaining explicitly **Deferred**, not resolved); zero changes to `src/ragtorch/core/events.py`/`errors.py`/`module.py` — confirmed via `git diff --stat` showing zero lines in any production file; post-merge `main` CI run [32061828395](https://github.com/payamfirouzfar/RAG-MODULE/actions/runs/32061828395), commit `7136959`, 522/522 on 3.10/3.11/3.12. **Precise framing, not to be paraphrased as "reentrancy is fully supported":** the library provides no reentrancy abstraction and makes no depth guarantee; deterministic testing found every legitimate (terminating, different-event) nesting pattern already works correctly, and the one failure mode (unconditional same-event recursion) is a clean, self-bounded, cheap `RecursionError`, not a hang or resource leak | `EVT-REENTRANT-001` remains fully open and Deferred (unchanged, re-confirmed rather than resolved); `EVT-RACE-001` (Step 21) remains separately tracked and unaffected -- concurrency and reentrancy are distinct concerns, this step introduced no threading/locking of any kind | `tests/unit/core/test_events.py` (16 new tests: 7 parametrized functions across both `EventBus`/`EventScope` covering R1/R3/R5/R5b/R7/R10/R12, plus 2 `Module.__call__`-specific tests for R14/R15 and the security-relevant self-bounded-cost characterization), `benchmarks/step22_event_reentrancy_audit.py` (direct/one-level-nested/recursive-to-failure publish cost, no threshold), `evaluation/step22-evaluation.md` |
| A75 | `CompositionGraph`'s full-revalidation-per-update cost (A61's named limitation) was audited against actual repository evidence, not assumption: `add_node`/`add_connection`/`remove_node`/`remove_connection` are called **nowhere** in `src/ragtorch` outside `composition.py` itself — every real consumer (`Block`, `ExecutionPlan`'s `plan()`, `RAGModule.from_graph`) constructs a `CompositionGraph` exactly once and never mutates it, confirmed by `grep -Rln` across the entire source tree. A dedicated six-workload benchmark found the only shape where full-revalidation cost compounds meaningfully — chained construction from empty, O(n²)-shaped, ~133ms total for a 1,000-node chain — has no real caller anywhere in this codebase; a single update against an already-large graph costs the same as reconstructing it from scratch at that size (~1.1ms at 1,000 nodes), directly confirming "update = full rebuild" as the precise characterization. A comparable-magnitude, per-call cost (`ExecutionPlan`'s `plan()` derivation, ~855µs at 1,000 nodes, Step 13) is already tolerated elsewhere in this same codebase without optimization (ADR-020 Q11), for the identical "no evidence it matters yet" reasoning this audit reaches independently. A design-space comparison (6 alternatives × 18 criteria) plus an adversarial review against the most plausible incremental-validation design found that incremental cycle detection specifically is a known-hard problem (tractable on edge addition, but removal generally requires re-deriving reachability from scratch anyway) that would trade a simple, fully-correct component for a more complex one with new correctness risk classes (stale indexes, branch/version state contamination) that do not exist today. Decision: no `ADR-024`, no production code change — the evidence did not support incremental validation | Step 23 audit (no new ADR; A61's own text remains historically frozen per this project's append-only convention — this row is the evidence record, not a rewrite); zero changes to `src/ragtorch/core/composition.py` or any other production file — confirmed via `git diff --stat` showing zero lines in any production file; post-merge `main` CI run [32063303843](https://github.com/payamfirouzfar/RAG-MODULE/actions/runs/32063303843), commit `ba726fb`, 522/522 on 3.10/3.11/3.12 (test count unchanged from Step 22 — audit-only, no new tests). **Precise framing, not to be paraphrased as "CompositionGraph is optimized" or "the performance problem is fixed":** no optimization was made; the audit found no evidence any optimization is needed, and documented precisely why, including the one synthetic workload (chained construction) where the current cost genuinely would compound if a real caller ever did this | A61 remains fully open and Deferred (unchanged, re-confirmed rather than resolved) — this row exists so a future audit that touches `composition.py` does not have to re-derive this evidence from scratch; revisit if a real consumer is ever added that performs chained/repeated `CompositionGraph` mutation at scale | `benchmarks/step23_composition_graph_update_audit.py` (six workloads: build-time chained construction, repeated branching, large-graph construction at 10/100/1,000/10,000 nodes, connection-heavy/fan-out graphs, repeated updates against an already-large graph), `evaluation/step23-evaluation.md` |
| A76 | ADR-010's hard rule (`isinstance(_, Component)` must never appear on a hot execution path) now has **automated regression enforcement**, not merely documentation + code review: a parametrized AST-based scan of `ragtorch.core.module`, `ragtorch.core.engine`, `ragtorch.core.sequential`, and `ragtorch.core.block` — the four files any hot execution path runs through — asserting zero such calls exist in any of them, reusing this project's own established AST-based source-inspection pattern (first used in Steps 7/8/10/12/16 for provider-independence checks) rather than introducing new infrastructure. Preceded by a full 12-candidate architecture gap discovery/priority audit (R1/R2/R3 provider-model-storage contracts, A3 `RAGModule` semantics, A6/A7 routing/fallback, A8 evaluation-gating, A9 snapshot serializer, A11 plugin boundary, A16 hot-path enforcement, A20 explicit ports, plus re-confirming A61/`EVT-RACE-001`/`EVT-REENTRANT-001` should stay Deferred) that found A16 was the only candidate with a real, currently-true invariant, zero real consumer needed (it is a static check, not a feature), low implementation complexity, and a directly reusable existing pattern — every other candidate investigated has zero real, synthetic, or planned-in-code consumer, confirmed via direct source search (`find`/`grep` across `src/ragtorch`), not assumption. No benchmark file: this is a static AST check with no runtime execution path, so there is no execution-time behavior to measure that would constitute real evidence — the existing `benchmarks/step6_component_contract.py` already measures the *runtime* `isinstance(module, Component)` cost as informational evidence for this same requirement, and remains the correct, sufficient record | Step 24 audit + implementation; no ADR required — this enforces an already-decided rule (ADR-010), introducing no new architectural decision; `tests/unit/core/test_component.py` — 6 new tests: a parametrized scan across the four hot-path files, plus two "test the test" self-checks (confirms the detector catches a real violation; confirms it does not false-positive on an unrelated `isinstance` call) — a first implementation attempt (`inspect.getsource` on a synthetic in-memory module) correctly failed and was fixed to parse real source text directly, caught before it could hide a false-positive-passing detector; manually verified against real source by injecting a synthetic violation into `sequential.py`'s actual source text and confirming detection at the correct line number; zero changes to `module.py`/`engine.py`/`sequential.py`/`block.py` — confirmed via `git diff --stat` showing zero lines in any production file; post-merge `main` CI run [32066462641](https://github.com/payamfirouzfar/RAG-MODULE/actions/runs/32066462641), commit `d0fde24`, 528/528 on 3.10/3.11/3.12 | none for the scoped contract — this closes A16 exactly as scoped (this codebase's own four hot-path files), not a general project-wide lint rule, which Step 24's own design-space comparison explicitly rejected as disproportionate to the evidence | `tests/unit/core/test_component.py` (`test_hot_path_never_checks_isinstance_component` parametrized ×4, `test_hot_path_enforcement_detector_actually_detects_a_violation`, `test_hot_path_enforcement_detector_ignores_unrelated_isinstance_calls`), `evaluation/step24-evaluation.md` |
| A77 | `ragtorch`'s packaged distribution artifacts (wheel + sdist) are now CI-verified as installable and importable outside the source checkout, not merely assumed from `python -m build` succeeding: a dedicated CI `packaging` job (Python 3.10/3.11/3.12) builds both artifacts, inspects their contents, installs the wheel into a fresh venv (no editable install, no dev deps), imports `ragtorch` and its full documented public API from that clean install, and runs a consumer smoke test through a real `Sequential` pipeline — proving the installed artifact works, not just the repository tree. Audit found and fixed one real defect: `.claude/scheduled_tasks.lock` (a local development artifact excluded only via a personal `git/info/exclude`, never the repository's own tracked `.gitignore`) was leaking into every sdist built from a clean clone, including in CI; fixed via `.gitignore` plus an explicit `[tool.hatch.build.targets.sdist]` allowlist as defense-in-depth (an experiment confirmed the `.gitignore` fix alone was sufficient for this specific defect — the allowlist is additional, not strictly required). Also fixed a stale placeholder `Homepage` URL and non-idiomatic license metadata (`license = {file=...}`, which embedded the full Apache-2.0 text into wheel METADATA, ~18KB) in favor of an SPDX `license` expression + `license-files`, shrinking METADATA to ~5.2KB. A first PR CI attempt (commit `e3d4698`) genuinely failed — `packaging (3.10)` hit `ModuleNotFoundError: No module named 'tomllib'` (stdlib-only from Python 3.11+) in a new dependency-audit test; fixed with a `tomli` backport fallback, verified locally against a real Python 3.10.11 interpreter before pushing again | Step 25 audit + implementation; no ADR required — packaging/release infrastructure, no architectural decision; `.gitignore`, `pyproject.toml` (sdist allowlist, SPDX license, Homepage URL fix, `tomli` conditional dev dependency), `.github/workflows/ci.yml` (new `packaging` job, 3.10/3.11/3.12), `README.md` (`## Install` section distinguishing dev install from real-wheel install, no PyPI claim), `tests/packaging/test_clean_install.py` (11 new tests, marked `packaging`, excluded from default `pytest` run); zero changes to any file under `src/ragtorch/` — confirmed via `gh pr view --json files`; PR [#32](https://github.com/payamfirouzfar/RAG-MODULE/pull/32); merge SHA `a7ee77195b225e27ce543ae65d1f7cd092e50a0c`; post-merge `main` CI run [32072645810](https://github.com/payamfirouzfar/RAG-MODULE/actions/runs/32072645810), 528/528 (`test`) and 11/11 (`packaging`) on all of 3.10/3.11/3.12, wheel inspected at 31 entries with all content checks passing | PyPI publishing itself remains explicitly out of scope and not implemented — this closes clean-install/CI verifiability only, not public distribution; README's general documentation currency (e.g. the `## Status` section's stale "Step 4" reference) remains untouched, explicitly out of scope for this step | `tests/packaging/test_clean_install.py`, `evaluation/step25-evaluation.md` |
| A78 | A dedicated, RAG-vocabulary-specific architecture-boundary discovery audit (embedding, vector, retriever, retrieval, document, chunk, splitter, loader, generator, llm, prompt, multimodal, image, vision, "graph rag", "knowledge graph", reranker, citation, provider, adapter, plugin — 21 keywords, exhaustive case-insensitive search across `src/`, `tests/`, `docs/`, `benchmarks/`, `evaluation/`) found **zero real production consumer** for any of the 12 candidate RAG concepts (document ingestion, chunking, embedding generation, vector storage, retrieval, reranking, prompt construction, LLM generation, citation/source tracking, multimodal input, image retrieval, graph retrieval) — independently reproducing and reinforcing A76's (Step 24) architecture-gap-audit conclusion using RAG-specific rather than architecture-gap vocabulary. The only test-only artifacts found are trivial `Retriever`/`Generator`/`Reranker` `Module` subclasses reused across 6+ test files purely to exercise unrelated framework mechanics (ports, snapshot, graph composition, event scoping) — not evidence of a needed contract. A 6-option × 20-criteria provider-boundary design-space comparison (interfaces-in-core / separate package / adapter-plugin / Protocol-structural / generic-Module-based / hybrid) and an 11-abstraction minimum-contract inventory (`Document`, `Chunk`, `Embedding`/`EmbeddingModel`, `VectorStore`, `Retriever`, `RetrievedDocument`/`RetrievalResult`, `Reranker`, `PromptTemplate`, `Generator`/`LLM`, `Citation`/`Source`, `RAGPipeline`) were both produced and preserved as evidence, but every abstraction failed the "is it necessary now?" gate — none were designed or implemented. `CompositionGraph` (ADR-016) was explicitly confirmed to carry zero retrieval/embedding/knowledge-graph semantics and must not be confused with a future application-level knowledge graph for Graph RAG. Decision: no ADR, no production code change — the evidence did not support defining or implementing any provider/RAG contract boundary yet | Step 26 audit (no new ADR; ADR-005 remains the sole governing decision on provider independence, unchanged); zero changes to `src/ragtorch/**` — confirmed via `git diff --stat` showing zero lines in any production file | R1/R2/R3/R6/R11 (provider/model/storage independence, replaceability, optional-dependency contracts) and A3/A6/A7/A8/A11 (RAGModule semantics, provider routing/fallback, evaluation-gating, plugin boundary) all remain fully open and Deferred, re-confirmed rather than resolved — this is the second independent audit (after A76) to reach that conclusion; the provider-boundary design-space comparison and minimum-contract inventory are preserved so a future step with a real consumer does not have to re-derive them from scratch | `evaluation/step26-evaluation.md` |
| A79 | A first real-consumer-discovery experiment built and ran a complete, deterministic, provider-independent toy RAG pipeline (documents → chunking → embedding → indexing → retrieval → context assembly → generation, `tests/discovery/rag_fakes.py`) using entirely local test doubles (no network, API key, external model, vector database, or third-party AI dependency), then adversarially tested it (21 discovery tests: provider replacement, dimensionality mismatch, empty corpus/query, duplicate documents, missing/large metadata, Unicode, provider-failure propagation, secret redaction, `Module`/`Sequential` compatibility). Discovered concrete couplings: embedding dimensionality is never assumed fixed anywhere in the pipeline; a `VectorStore`'s stored embeddings and a query's embedding vector must come from the same embedder or search fails loudly (a real, unavoidable coupling, not a design flaw); a `Retriever` does not require an `Embedder`/`VectorStore` dependency at all (proven via a working keyword-based `InvertedIndexVectorStore` + `KeywordRetriever`); `VectorStore.search(query_vector, top_k)`'s own signature already assumes vector-based retrieval, meaning `VectorStore` and `Retriever` should likely not be conflated as one future contract. A 6-option × 21-criteria architecture comparison (RAG-in-core / `ragtorch.rag` / `ragtorch.adapters` / core+rag+adapters split / RAG entirely external via `Component` / hybrid) and a 7-type data-contract inventory (`Document`, `Chunk`, `Embedding`, `ScoredChunk`, `RetrievalResult`, `GenerationResult` — all immutable, all JSON-representable; `PromptTemplate` was tried and found unnecessary, a plain `str` sufficed) were both produced and preserved as evidence. Decision: no ADR, no production code change — despite the pipeline being real, working, and adversarially tested, its consumer is still synthetic (constructed by this step itself to generate evidence), not an actual external application, so Step 26's own bar ("no provider abstraction without a real RAG consumer") remains unmet | Step 27 audit + discovery experiment; no ADR required — ADR-005 remains the sole governing decision on provider independence, unchanged; zero changes to `src/ragtorch/**` — confirmed via `git diff --stat` showing zero lines in any production file | R1/R2/R3/R6/R11 and A3/A6/A7/A8/A11 all remain fully open and Deferred, re-confirmed rather than resolved — the third consecutive step (after A76, A78) to reach that conclusion, now with running-code evidence rather than only design comparison; two new concrete open risks recorded: duplicate `Document.id` values would silently overwrite prior chunks in a naive store (no identity contract exists to prevent this), and the experiment's `Document.text: str` shape hard-codes a text-only assumption not yet tested against any multimodal case | `tests/discovery/rag_fakes.py`, `tests/discovery/test_rag_consumer.py` (21 tests), `benchmarks/step27_rag_discovery_pipeline.py`, `evaluation/step27-evaluation.md` |
| A80 | `ragtorch` now has a complete, CI-verifiable public-package release pipeline, fully separated from PR/branch CI: `.github/workflows/release.yml` builds and independently re-validates the wheel/sdist from a tagged commit (refusing to proceed if the git tag doesn't exactly match `pyproject.toml`'s version), publishes via PyPI Trusted Publishing (OIDC, `permissions: id-token: write` — no PyPI API token stored in any GitHub secret), then verifies the actual publication by installing the just-published version from PyPI itself into a fresh environment and running a smoke test — publication is only triggered by a `v*` tag push or an explicit manual confirmation, never by a pull request or a plain push to `main`. ADR-024 makes SemVer's pre-1.0 allowance explicit (while `MAJOR` is `0`, a `MINOR` bump may break the public API; `PATCH` bumps are backward-compatible only) and documents why `pyproject.toml`'s manually-set `version` stays the single source of truth rather than dynamic/VCS-derived versioning. A real, previously-undetected defect was found and fixed during this step's own audit: `ragtorch.__version__` was a second, independently hardcoded string in `src/ragtorch/__init__.py` that had silently drifted from `pyproject.toml`'s `version` field (`__version__` still said `"0.4.0"` after `pyproject.toml` had already been bumped to `0.5.0` for this same step) — fixed by deriving `__version__` from `importlib.metadata.version("ragtorch")` at import time instead, which reads the metadata `hatchling` already generates correctly from `pyproject.toml`, eliminating the dual-source-of-truth class of bug entirely rather than just correcting the one instance. Two real packaging-test coverage gaps were also found and fixed: `ExecutionEngine` and `ragtorch.evaluation` were previously only ever imported, never actually executed, against a clean installed wheel — two new tests now run a real `ExecutionEngine.execute()` call and a real `Evaluator.evaluate()` call against the installed artifact. README rewritten to explicitly separate "what ragtorch is today" (execution kernel) from "what ragtorch is not yet" (no embeddings/vector stores/LLM providers/etc.), so the public package listing never overclaims RAG-provider functionality that doesn't exist. Publication to PyPI itself was **not completed in this step** — it requires either a PyPI API token or PyPI Trusted Publishing configured by a PyPI account owner on pypi.org, neither of which is available in this environment; this is an explicit stop condition, not an oversight, and Step 28 does not claim a live PyPI release exists | Step 28 audit + implementation; ADR-024 (Proposed); `pyproject.toml` (version 0.5.0, keywords, refined classifiers, project URLs), `src/ragtorch/__init__.py` (`__version__` now derived from installed package metadata, not a hardcoded string — one line of production code changed, the only `src/ragtorch/**` change in this step), `.github/workflows/release.yml` (new, build/validate/publish/verify jobs), `docs/RELEASING.md` (deterministic release procedure), `tests/packaging/test_clean_install.py` (+2 tests: ExecutionEngine and evaluation API from clean install), `benchmarks/step28_packaging_properties.py` (build/install/import time, artifact size — measured, no threshold), `README.md`, `CHANGELOG.md` | PyPI publication itself remains not-yet-completed — this closes release-pipeline readiness and correctness only, not "the package is live on PyPI"; Steps 5-27's substantial work shipped without corresponding CHANGELOG entries or version bumps (the version stayed frozen at 0.4.0 across ~23 steps of real, merged work) — this gap is recorded here rather than fabricated retroactively with invented intermediate version numbers; R1/R2/R3/R6/R11 and A3/A6/A7/A8/A11 remain untouched and fully open/Deferred, exactly as before — this step is packaging/release infrastructure only and resolves no RAG-architecture requirement | `.github/workflows/release.yml`, `docs/RELEASING.md`, `docs/architecture/decisions/ADR-024-versioning-and-release-policy.md`, `tests/packaging/test_clean_install.py`, `benchmarks/step28_packaging_properties.py`, `evaluation/step28-evaluation.md` |
| A81 | The release pipeline A80 recorded as built-but-unpublished is now confirmed publicly published: `ragmodel` version `0.5.0` is live on PyPI (https://pypi.org/project/ragmodel/, confirmed via direct `curl https://pypi.org/pypi/ragmodel/json` query returning `{"info": {"name": "ragmodel", "version": "0.5.0"}}`, not merely CI logs), published under the PyPI distribution name `ragmodel` per the repository owner's explicit choice — `import ragtorch` is completely unaffected (zero source code changes; PyPI allows the distribution name and Python import name to differ). Independently verified from a brand-new, isolated virtual environment installing the exact pinned version from the real public index (`pip install "ragmodel==0.5.0"`), outside any source checkout: `ragtorch.__version__ == "0.5.0"`, `ragtorch.__file__` resolves to `site-packages`, and a full functional smoke test (`RAGConfig`, a `Retriever`→`Generator` `Sequential` pipeline, `ExecutionEngine` at `DEBUG` observability producing a `SUCCEEDED` run with a renderable trace, and the `ragtorch.evaluation` API) all passed against the published artifact itself. A real, previously-undetected defect was found and fixed during the rename that made this possible: `ragtorch.__version__`'s `importlib.metadata` lookup was querying the distribution by the import name (`"ragtorch"`) rather than the actual installed distribution name (`"ragmodel"`), which raises `PackageNotFoundError` and silently falls back to a `"0.0.0+unknown"` sentinel — caught by actually building and installing the renamed wheel into a clean venv, not assumed correct. A second real defect was found and fixed in the release workflow itself: the first actual publish run's `verify-publication` job failed to create the GitHub Release with a `403 Resource not accessible by integration` error, root-caused to a missing `contents: write` permission grant on that job (the default `GITHUB_TOKEN` has none without an explicit grant) — fixed with a minimally-scoped grant on just that job, confirmed via a subsequent green CI run, and the missing `v0.5.0` GitHub Release was created directly (`gh release create`) as a one-time remediation since PyPI does not permit re-publishing an already-uploaded version to retry the automated path | Step 28 closure (human-controlled publication gate performed by the repository owner: PyPI account creation, 2FA enablement, Trusted Publisher registration for `payamfirouzfar/RAG-MODULE`/`release.yml`/`pypi` environment under the project name `ragmodel`); `pyproject.toml` (`name` field), `src/ragtorch/__init__.py` (`__version__` resolution fixed to query the correct distribution name — the only `src/ragtorch/**` change), `.github/workflows/release.yml` (PyPI project URL updated to `ragmodel`, `verify-publication` job's install target updated, `contents: write` permission added), `README.md`/`docs/RELEASING.md`/`CHANGELOG.md` (distribution/import name split documented, real PyPI availability now stated as fact rather than aspiration); PR [#36](https://github.com/payamfirouzfar/RAG-MODULE/pull/36) (rename, merge `1a51f8a`) and PR [#37](https://github.com/payamfirouzfar/RAG-MODULE/pull/37) (permissions fix, merge `5ed4371`), both CI-green on all 6 jobs across 3.10/3.11/3.12; publication workflow run [32114922290](https://github.com/payamfirouzfar/RAG-MODULE/actions/runs/32114922290) (`build-and-validate` and `publish` succeeded; `verify-publication` partially failed as described above, with its PyPI-install smoke test step itself having already passed before the unrelated GitHub-release step failed); GitHub release: https://github.com/payamfirouzfar/RAG-MODULE/releases/tag/v0.5.0 | None of R1/R2/R3/R6/R11 or A3/A6/A7/A8/A11 are affected or resolved by this row — publication is packaging/release-infrastructure evidence only, not a RAG-architecture requirement; the `ragtorch`-vs-`ragmodel` distribution/import name split is now a permanent, real characteristic of this package that any future step involving packaging, documentation, or dependency instructions must account for (do not assume the PyPI name matches the import name) | `evaluation/step28-evaluation.md` (28R/28S sections) |
| A82 | Repository hygiene pass, evidence-driven (no real RAG/serialization consumer found — see Step 29 discovery audit reproducing and reinforcing A76/A78/A79's conclusions using a fourth independent method): closed stale PR #1 (`feat/step5-architecture-foundation` → `main`), open since before this project's current history began, whose content (execution-scoped observability) was independently redesigned and already shipped as ADR-022/023 many steps ago — first flagged during Step 24's audit but not acted on until now. Corrected a real, verified documentation defect in `docs/architecture/v0.1-architecture.md` §13: the text claimed a "package rename from `ragtorch` to `ragmodule`" remained "deferred," which as of Step 28 is both stale (the actual PyPI-distribution rename already happened) and factually wrong (the real target name is `ragmodel`, not `ragmodule`, and the Python import name `ragtorch` was never renamed and has no rename plan) — fixed by appending a dated correction pointing to ADR-024/`evaluation/step28-evaluation.md`, with the original text preserved unmodified below it rather than deleted, matching this project's append-only/historical-preservation convention applied everywhere else | Step 29 audit + housekeeping; no ADR required — closing a stale PR and correcting documentation are not architectural decisions; zero changes to `src/ragtorch/**`; `docs/architecture/v0.1-architecture.md` (one section corrected); GitHub PR #1 closed (not merged) with an explanatory comment | R1/R2/R3/R6/R9/R11 and A3/A6/A7/A8/A11 all remain untouched, fully open and Deferred — this row resolves no RAG-architecture requirement, it is repository hygiene only; R9's "Run artifact schema incomplete" gap was investigated during this step's discovery phase and found to match a consistently-applied, deliberate, evidence-based non-goal already repeated across `CompositionGraph`/`Block`/`ExecutionPlan`/`Connection` (all "kept serialization-friendly, not implemented") rather than a new or overlooked gap — no action taken, explicitly not a resolution of R9 | `evaluation/step29-evaluation.md` |

## Step 5 status

Step 5's first implementation gap is now closed: the event-context overhead benchmark exists, the pre-fix observation is recorded, and CI executes and archives the benchmark for every supported Python version. The no-context execution path was also changed to avoid constructing execution-identity kwargs when no context is supplied.

The benchmark intentionally does **not** introduce an arbitrary hard microsecond threshold yet. A stable CI baseline must be collected first; then a justified regression budget can be added.

## Step 6 status

The Component contract (A1, A13-A16) is implemented per ADR-010:
`ragtorch.core.component.Component`, a minimal `@runtime_checkable Protocol`
with exactly `name`, `component_type`, and `__call__`. `Module` gains two
properties and structurally satisfies it with zero inheritance migration
and zero call-site changes anywhere in the existing 148 tests. An
independent, non-`Module` class also satisfies it, proving the contract is
genuinely structural rather than accidentally testing only `Module`.

`capabilities`, `configuration`, async/streaming variants, a component
registry, and Block/Architecture composition remain explicit non-goals for
this step (see ADR-010) — deferred until a real component exists to prove
their shape, not designed speculatively now.

## Step 7 status

Component ports (A17-A20) are implemented per ADR-011:
`InputPort`/`OutputPort` (frozen dataclasses, construction-time type
validation) and `is_compatible()` (single `issubclass()` check covering
both exact-match and subtype compatibility). `Component`, `Module`,
`ExecutionEngine`, `ExecutionContext`, and `Sequential` are all
unchanged — zero lines touched, all 159 pre-existing tests pass
unmodified. `ValidationError` (already existing) is reused; no new
exception type introduced. Ports are never derived from `__call__`
annotations via reflection — verified as a real code-execution surface
(`typing.get_type_hints()` evaluates string annotations) before
deciding against it, not assumed safe.

Generic-parameter-aware compatibility, `Union`/structural typing, ports
attached to `Component`, and a `Block`/graph builder that enforces
compatibility all remain explicit non-goals (see ADR-011) — deferred
until a real component demonstrates the need.

## Step 8 status

The architecture snapshot (A9, A21-A28) is implemented per ADR-012:
`ragtorch.core.inspection.ArchitectureSnapshot`, built by `snapshot()`
in a single logical pass over `Module.named_modules()`/`named_children()`
(one dict-building pass, one children-building pass — down from an
initial three-pass draft that both wasted work and, when "optimized" to
avoid the third pass by hand-reconstructing dotted paths, introduced a
real bug at the root level; caught before commit by verifying output
against `named_modules()` directly, not merely by re-running the
existing test suite, which still passed with the bug present since it
didn't specifically target that path). `Module.inspect()` is migrated to
render text from this snapshot rather than an independent walk of
`_modules` — its public string output is byte-for-byte unchanged
(existing Step 1 test passes unmodified). `Component`, `Module.__call__`,
`ExecutionEngine`, `ExecutionContext`, `Sequential`, and `ports.py` are
all unchanged — zero lines touched. `snapshot()` never executes a
component and never reads arbitrary instance state (`vars()`/`__dict__`)
— both proven by dedicated tests, not merely asserted.

A real, honestly-measured performance cost: `inspect()` is now ~100µs
p50 for a 10-module tree, versus Step 1's original ~13.4µs for an
11-module tree — roughly 7x higher, from constructing immutable
dataclass instances the old string-only implementation never allocated.
Accepted as a deliberate tradeoff (`inspect()` is diagnostic, never a
hot path; still 3-4 orders of magnitude below any real RAG operation),
not silently absorbed — see `evaluation/step8-evaluation.md` for the
full accounting.

Data-flow/graph edges, port auto-discovery, serialization (JSON/etc.),
and additional renderers (Mermaid/Graphviz) all remain explicit
non-goals (see ADR-012) — deferred until a real composition/graph
contract or consumer exists to justify their shape.

## Step 9 status

Architecture snapshot validation (A29-A36) is implemented per ADR-013:
`ragtorch.core.architecture.validate_snapshot()`, a ten-rule
non-empty-rooted-tree contract checked in a fixed order (empty →
duplicate IDs → dangling references → self-containment → multiple
parents → root count → reachability), `None` on success,
`ValidationError` (reused, no new exception type) on the first
violation. `Component`, `Module`, `ExecutionEngine`, `ExecutionContext`,
`Sequential`, `ports.py`, and `inspection.py` are all unchanged — zero
lines touched; `architecture.py` is a new, standalone module.

Two Staff-review corrections shaped the final contract before any code
was written: "exactly one root" alone is insufficient to prove a tree
(a diamond — `A→B`, `A→C`, `B→D`, `C→D` — has one root and zero
duplicate IDs but a doubly-parented node), and a disconnected cycle is
caught by reachability, not root-counting, since neither cycle node has
zero incoming edges. Both were verified by direct construction before
being frozen into the ADR, and both now have dedicated regression
tests. No separate cycle-detection algorithm was implemented — proven,
not merely assumed, that the existing rules already imply acyclicity
for a valid tree.

An explicit, named limitation carried forward from the audit: the
pre-existing `Module` cycle-registration gap (`a.child = b; b.child = a`
succeeding, then `RecursionError`-ing on traversal) remains unaddressed
— `validate_snapshot()` operates on the snapshot boundary only and
cannot protect against a tree `snapshot()` itself couldn't finish
building. Frozen as an explicit Non-goal (ADR-013), not silently
patched here.

Benchmarked scaling (1/10/100/1,000-node trees) confirms the O(N+E)
design empirically: node count growing 10x costs roughly 8-10x time,
not ~100x — see `evaluation/step9-evaluation.md`.

## Step 10 status

Composition preconditions (A37-A44) are implemented per ADR-014:
`ragtorch.core.ports.check_connection()`, a raising precondition
wrapper — "ask" (`is_compatible()`, `bool`) vs. "enforce"
(`check_connection()`, `None`/raises `ValidationError`). Its body is
exactly `if not is_compatible(...): raise ...`, so `is_compatible()`
remains the single source of truth for compatibility semantics; no
parallel `issubclass()` logic was written. `Component`, `Module`,
`Sequential`, `ExecutionEngine`, `ExecutionContext`,
`ArchitectureSnapshot`, and `validate_snapshot()` are all unchanged —
zero lines touched; `check_connection()` was added to the existing
`ports.py` alongside `is_compatible()`, not a new module.

This step deliberately does **not** integrate `check_connection()`
with `ArchitectureSnapshot`/`validate_snapshot()`, and does not
introduce a `Block`/composite type or graph executor — it proves the
port-level precondition primitive in isolation, per the project's
consistent "one primitive per step" discipline (Steps 6-9). An
explicit regression pair proves `is_compatible()`'s pre-existing
`bool` contract is unaffected by the addition, not merely assumed
from "we didn't edit that function."

Benchmarked overhead (`benchmarks/step10_composition_preconditions.py`):
`check_connection(valid)` is indistinguishable from
`is_compatible(valid)` (~0.1µs both); `check_connection(invalid)`
costs ~0.7µs more, consistent with the added f-string construction and
exception raise on the failure path — see
`evaluation/step10-evaluation.md`.

## Step 11 status

Connection identity and directionality (A45-A51) is implemented per
ADR-015: `ragtorch.core.connection.Connection`, a frozen value type
persisting a single, directed, validated data-flow edge
(`source_node_id`, `source_port: OutputPort`) → (`target_node_id`,
`target_port: InputPort`). `__post_init__` enforces four invariants —
non-empty node IDs, correct port role for each end — before delegating
compatibility to the unchanged `check_connection()` (Step 10).
`Component`, `Module`, `Sequential`, `ExecutionEngine`,
`ExecutionContext`, `ArchitectureSnapshot`, `validate_snapshot()`, and
`ports.py` itself are all unchanged — zero lines touched;
`connection.py` is a new, standalone module.

A Staff-review correction shaped the final contract before any code
was written: the first draft relied on field type annotations
(`source_port: OutputPort`, `target_port: InputPort`) alone to
guarantee directionality, which is not a runtime guarantee in Python —
and `check_connection()` (ADR-014) explicitly does not normalize
non-`Port` arguments into `ValidationError`, so an annotation-only
`Connection` could have silently constructed with its ends swapped.
Two explicit `isinstance` checks were added at the `Connection`
boundary specifically to close this, without reopening ADR-014's own
documented non-guarantee for `check_connection()` itself.

Fan-out/fan-in cardinality is deliberately left unconstrained (proven
by dedicated tests, not merely unaddressed) — whether either pattern
should be permitted, and where that rule would live, is explicit
future work for a collection/`Block` type built on top of `Connection`,
not decided here. No graph type, no cycles, no execution order, no
serialization: this step proves the element, not the aggregate.

Benchmarked overhead
(`benchmarks/step11_connections.py`): `Connection(valid)` costs ~1.9µs
more than `check_connection()` alone at p50 — the frozen-dataclass
construction path is not free, even though each individual check is
cheap — reported as measured rather than left at an earlier,
incorrect "should be small" prediction. See
`evaluation/step11-evaluation.md`.

## Step 12 status

Composition graph validation (A52-A64) is implemented per ADR-016:
`ragtorch.core.composition.CompositionGraph`/`GraphNode`, an immutable,
validated collection of nodes and `Connection`s enforcing nine
canonical invariants in fixed order — unique node ids, referential
integrity, inherited directionality/compatibility from `Connection`,
no duplicate connections, fan-in ≤ 1 per input port (fan-out
unrestricted), acyclicity, and no dangling connections after node
removal. `Component`, `Module`, `Sequential`, `ExecutionEngine`,
`ExecutionContext`, `ArchitectureSnapshot`, `validate_snapshot()`,
`ports.py`, and `connection.py` are all unchanged — zero lines
touched; `composition.py` is a new, standalone module.

Two Staff-review corrections shaped the final contract. First:
`GraphNode.id` is explicitly graph-local identity, deliberately not
derived from or coupled to `ArchitectureNode.id` (ADR-012's dotted
`named_modules()` path) — an unrelated `Module`-tree refactor (an
attribute rename, a tree restructuring) must never silently break
composition-graph identity, which matters for future caching,
serialization, diffing, and reproducibility use cases. Second: cycle
detection is scoped as a property of *this* graph type describing
ordinary data-flow composition, not a universal invariant for every
future RAG-MODULE graph concept — a future `Loop`/`Retry` primitive is
explicit higher-level control-flow work, not an exception carved into
`CompositionGraph` itself.

A real bug was caught by this step's own benchmark, not by unit tests:
the first `_has_cycle()` implementation used recursive DFS, which
raised `RecursionError` on a 1,000-node linear chain (every unit test
used ≤4 nodes and never exercised this path). Fixed by rewriting it as
an iterative Kahn's-algorithm topological sort before commit — concrete
evidence for why this project benchmarks at multiple scales rather
than trusting correctness proven only at a single small size.

Functional updates (`add_node`/`add_connection`/`remove_node`/
`remove_connection`) each return a new, independently-validated graph
rather than mutating `self`, consistent with every prior structural
type's immutability guarantee — but each update re-validates the
entire resulting graph rather than incrementally checking only the
delta, an explicit, measured, and accepted tradeoff (see
`evaluation/step12-evaluation.md` Limitations), not a silently-absorbed
cost.

Benchmarked scaling (10/100/1,000-node linear chains) confirms the
O(N+E) design empirically: node count growing 10x costs roughly 10-12x
time, not ~100x — see `evaluation/step12-evaluation.md`.

## Step 13 status

Execution planning (A65) is implemented per ADR-017:
`ragtorch.core.execution_plan.plan()`, deriving a deterministic
`ExecutionPlan` (ordered `ExecutionStep`s, each carrying `node_id` and
direct `dependencies`) from a valid `CompositionGraph` via Kahn's
algorithm with an explicit FIFO `collections.deque` ready queue.
`Component`, `Module`, `Sequential`, `ExecutionEngine`,
`ExecutionContext`, `ArchitectureSnapshot`, `CompositionGraph`
(including `_has_cycle()`, whose LIFO shape was deliberately left
unchanged), and `Connection` are all unchanged — zero lines touched;
`execution_plan.py` is a new, standalone module.

This step is the first in the project to go through **two rounds of
adversarial architectural review before any implementation existed**.
13B.3 found one BLOCKER (ADV-01): the ADR's first draft claimed
`plan()` reuses `CompositionGraph._has_cycle()`'s exact traversal
shape for its declared-order tie-break, which direct construction
proved false — `_has_cycle()`'s queue is LIFO, and would have silently
reversed ties relative to the declared order the ADR promised. 13B.5,
re-attacking the corrected FIFO design rather than assuming the fix
was sufficient, found a second, more subtle issue (ADV-04, MINOR):
"tie-broken by graph.nodes order" is precise only for nodes ready at
the very start of planning — nodes that become ready mid-traversal are
ordered by `graph.connections` discovery order instead, a genuinely
different (though equally deterministic) rule. Both findings are
reflected as named regression tests, not just prose, so a future
refactor cannot silently reintroduce either defect.

`plan()` does not re-validate graph structure, never executes a
component, never mutates or retains a reference to its source graph,
and contains no runtime objects — proven by dedicated tests
(`ExplodingComponent`, `dataclasses.fields()` introspection,
before/after snapshot comparison), not merely asserted from the ADR's
design intent.

Benchmarked scaling (10/100/1,000/10,000-node linear chains and wide
fan-out, plus two diamond-heavy shapes) confirms roughly linear growth
through 1,000 nodes with a modest superlinear tail at 10,000 —
reported as measured, not smoothed over — see
`evaluation/step13-evaluation.md`. No `RecursionError` or other
scale-dependent failure occurred at any tested size, since `plan()`
used an iterative `deque`-based algorithm from its first
implementation, unlike Step 12's `_has_cycle()`, which required a
post-hoc fix after failing at 1,000 nodes.

## Next priority

1. ~~Add concurrency tests around event identity before moving event delivery to execution-scoped ownership.~~ **Done — Step 18, ADR-022 (Accepted), A70.**
2. Design a `Block` type that consumes `CompositionGraph` (Step 12) as its validated structural foundation — the payoff every primitive since Step 6 exists to enable.
3. Design an executor that consumes `ExecutionPlan` (Step 13) — synchronous first, with the data model already proven not to foreclose async/parallel execution later.
4. ~~Consider whether the pre-existing `Module` cycle-registration gap (A29's named limitation) is worth a dedicated future ADR.~~ **Done — Step 19, no ADR required (reasoned, not assumed — see `evaluation/step19-evaluation.md` 19D), A71.**
5. Consider whether `CompositionGraph`'s full-revalidation-per-update cost (A61's named limitation) is worth an incremental-validation optimization, once a real workload demonstrates it matters.
6. Keep the milestone rule: design → ADR → contract → implementation → tests → benchmark → evaluation → CI → documentation.

The Component migration is intentionally **not** a rename of `Module`. We
first proved the contract and compatibility boundary (Step 6) before any
future migration decision is made. Ports (Step 7) are additive,
optional metadata layered beside Component, not a growth of its core
contract. The architecture snapshot (Step 8) gives Ports and any future
Block/graph layer a canonical structural representation to build on,
without prematurely committing to data-flow or serialization semantics.
Architecture validation (Step 9) gives that representation a fail-fast
correctness check, independent of execution, before any future
composition layer needs to reason about it.
